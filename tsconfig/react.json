{
  "extends": "./base.json",
  "compilerOptions": {
    // Vite owns compilation and output; tsc is type-check only
    "noEmit": true,
    "target": "ESNext",

    "lib": ["ES2023", "DOM", "DOM.Iterable"],
    "jsx": "react-jsx",
    "module": "ESNext",
    "moduleResolution": "bundler"
  }
}
